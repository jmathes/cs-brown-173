# Don't have time to figure out how this works!

defvar i=false in {
    if ==(
        (
        if (i = ==(i, false))
        then
            i
        else
            i
        )
        ,
        (
        if (i = ==(i, false))
        then
            i
        else
            i
        )
    )
    then
        "true"
    else
        "false"
}
